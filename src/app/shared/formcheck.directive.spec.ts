import { FormcheckDirective } from './formcheck.directive';

describe('FormcheckDirective', () => {
  it('should create an instance', () => {
    const directive = new FormcheckDirective();
    expect(directive).toBeTruthy();
  });
});
